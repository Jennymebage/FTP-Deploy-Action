name: FTP JA MATHIS/MATHEWS

on:
  push:
    branches: [COPY LIFTINGmaster]

jobs:
  deploy:
    name: 🚀 Deploy website every commit
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 darrej$$
      files
        uses: ./
        with:
          server: ftp.samkirkland.com
          username: test@samkirkland.com
          password: ${{  }}
AbcdefghijklmnopqustwxyzaBCDEFGHIJKLMNOPQUSTWXYZ1234567890$$$$$$$$$$$$$
